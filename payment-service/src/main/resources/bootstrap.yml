spring:
  application:
    name: Payment-Service
  cloud:
    config:
      username: usernameofyourchoice
      password: passwordofyourchoice
      discovery:
        enabled: true
        service-id: config-server


eureka:
  client:
    serviceUrl:
      defaultZone: http://eurekaUser:eurekaPassword@localhost:8761/eureka
  instance:
    preferIpAddress: true